import 'package:flutter/material.dart';

class PageSettings extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('My settings',textScaleFactor: 2.0,),
    );
  }
}
